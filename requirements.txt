# The Analyst Copilot — dependencies
# Install into a project venv, never system Python:
#   python -m venv .venv
#   .venv\Scripts\activate          (Windows)   |   source .venv/bin/activate  (macOS/Linux)
#   pip install -r requirements.txt
#   pip install -e .

# ── HTML / XBRL parsing ──────────────────────────────────────────────────────
lxml>=5.0                 # DOM walk, anchors, table grids. MUST be used for anchor
                          # extraction — a regex probe misses them (Microsoft: 33 anchors
                          # missed because <a> wraps >200 chars of nested <span>).

# ── Storage: PostgreSQL + pgvector ───────────────────────────────────────────
psycopg[binary]>=3.1
pgvector>=0.2.5

# ── Retrieval ────────────────────────────────────────────────────────────────
rank-bm25>=0.2.2          # In-memory BM25. Azure Postgres has NO BM25 extension
                          # (pg_search/ParadeDB absent; ts_rank_cd is not BM25).
                          # Measured: indexes all 8,600 pages in 1.7 s.

# ── Model APIs ───────────────────────────────────────────────────────────────
openai>=1.40              # gpt-5-mini + text-embedding-3-small via Azure AI Foundry
httpx>=0.27               # Cohere rerank serverless endpoint

# ── Service ──────────────────────────────────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9   # file upload for "Add filing"

# ── Core ─────────────────────────────────────────────────────────────────────
pydantic>=2.6
pyyaml>=6.0               # config.yaml, formula_book.yaml, company_aliases.yaml
python-dotenv>=1.0

# ── Dev ──────────────────────────────────────────────────────────────────────
pytest>=8.0

# ── DELIBERATELY NOT ADDED ───────────────────────────────────────────────────
# arelle-release — XBRL is extracted with plain lxml over <ix:nonFraction>.
#                  No heavy standards-aware parser needed for concept+period lookup.
# sec-parser     — its repository states it is no longer maintained. Borrow the
#                  heuristics, not the dependency.
# torch / sentence-transformers — embeddings and reranking are hosted.
# LangChain / LlamaIndex — control flow is ordinary Python.
